name: Release

on:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - runtimeIdentifier: win-x64
            os: windows-latest
          - runtimeIdentifier: win-x86
            os: windows-latest
          - runtimeIdentifier: linux-x64
            os: ubuntu-latest
          - runtimeIdentifier: linux-arm
            os: ubuntu-latest
          - runtimeIdentifier: linux-arm64
            os: ubuntu-latest
          - runtimeIdentifier: osx-x64
            os: macos-13
          - runtimeIdentifier: osx-arm64
            os: macos-14

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Install Linux ARM Cross-compilation Dependencies
      if: matrix.os == 'ubuntu-latest' && (matrix.runtimeIdentifier == 'linux-arm' || matrix.runtimeIdentifier == 'linux-arm64')
      run: |
        sudo apt-get update
        sudo apt-get install -y clang zlib1g-dev
        if [ "${{ matrix.runtimeIdentifier }}" == "linux-arm64" ]; then
          sudo apt-get install -y gcc-aarch64-linux-gnu zlib1g-dev-arm64-cross
        elif [ "${{ matrix.runtimeIdentifier }}" == "linux-arm" ]; then
          sudo apt-get install -y gcc-arm-linux-gnueabihf zlib1g-dev-armhf-cross
        fi

    - name: Publish
      run: dotnet publish MehguViewer.Core.csproj -c Release -r ${{ matrix.runtimeIdentifier }} -o ./publish/${{ matrix.runtimeIdentifier }} /p:AssemblyName=MehguViewer.Core_${{ matrix.runtimeIdentifier }}_net9.0

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MehguViewer.Core_${{ matrix.runtimeIdentifier }}_net9.0
        path: ./publish/${{ matrix.runtimeIdentifier }}
